<%- include("../partials/head", { title }) %>
<%- include("../partials/nav") %>

<main class="wrap">
  <section class="pageHead" data-reveal>
    <h1 class="pageTitle">404</h1>
    <p class="pageSub">We couldn’t find that page.</p>
  </section>

  <section class="card big" data-reveal>
    <div class="cardText">Try heading back home—or log in if you were trying to reach your dashboard.</div>
    <div class="cta" style="margin-top:12px">
      <a class="btn primary" href="/">Home</a>
      <a class="btn" href="/login">Login</a>
    </div>
  </section>
</main>

<%- include("../partials/footer") %>
